include = ["**/*.toml"]
exclude = ["**/node_modules/**", "target/**"]

# Keep deploy configs compact: one rule per line like ['match', 'target'].
[[rule]]
include = ["deploy/*.toml"]

[rule.formatting]
array_auto_expand = false
array_auto_collapse = true
reorder_keys = false
reorder_arrays = false
